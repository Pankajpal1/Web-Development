<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= found.name %></title>
    </head>
    <body>
        <h1><%= found.name %></h1>
        <ul>
            <li>Price: $<%= found.price %></li>
            <li>
                Category:
                <a href="/products?category=<%= found.category %>"
                    ><%= found.category %></a
                >
            </li>
        </ul>
        <h3>Farm: <a href="/farms/<%= found.farm[0]._id %>"><%= found.farm[0].name %></a></h3>

        <a href="/products/<%= found._id %>/edit">Edit Product</a>
        <a href="/products">Go to Home</a>

        <form action="/products/<%= found._id %>?_method=DELETE" method="post">
            <button>Delete</button>
        </form>
    </body>
</html>
